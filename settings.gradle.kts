pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "ExperimentContextReceivers"
include(":androidApp")
include(":shared")